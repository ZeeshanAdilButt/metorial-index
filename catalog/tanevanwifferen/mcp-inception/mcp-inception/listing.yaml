description: TypeScript-based MCP server that enables calling another MCP
  client, allowing for task delegation and context window offloading. Supports
  parallel and map-reduce execution of tasks.
skills:
  - Delegate tasks
  - Offload context windows
  - Parallel execution of tasks
  - Map-reduce execution
  - Simple LLM query system
