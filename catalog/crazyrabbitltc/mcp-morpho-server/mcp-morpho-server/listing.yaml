description: Query Morpho's GraphQL API to access market data, vaults, and
  positions. Supports historical data, pagination, ordering, filtering, and
  validation.
skills:
  - Query market data
  - Access vaults and positions
  - Retrieve historical APY data
  - Provide oracle information
  - Handle errors and type safety
