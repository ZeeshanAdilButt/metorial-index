description: Aggregates multiple MCP resource servers through a single HTTP
  endpoint, enabling unified access to various MCP clients. Supports real-time
  updates with SSE and offers flexible configuration options for different
  client types.
skills:
  - Proxy multiple MCP clients
  - Provide real-time updates with SSE
  - Flexible configuration for client types
  - Aggregate tools and capabilities from multiple sources
