description: Provides standardized SQLite database operations through an MCP
  interface, enabling efficient execution of SQL commands, table management, and
  business insights memo tracking. Supports both in-memory and file-based
  storage configurations.
skills:
  - Perform SQL operations (SELECT, INSERT, UPDATE, DELETE)
  - Manage database tables (CREATE, LIST, DESCRIBE)
  - Track business insights memos
  - Support Docker deployment for integration
