description: Fetches web content and converts it to markdown format for easy
  processing by LLMs. It retrieves content from specified URLs and allows for
  extraction in manageable chunks by specifying character indexes.
skills:
  - Fetch web content
  - Convert HTML to markdown
  - Content extraction with truncation
  - Specify starting index for content
