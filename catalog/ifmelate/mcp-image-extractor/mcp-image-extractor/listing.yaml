description: Extracts images from local files and URLs, processing them into
  base64 format for analysis by large language models (LLMs). Suitable for
  analyzing image-based data, such as screenshots from tests.
skills:
  - Extract images from local files
  - Extract images from URLs
  - Process base64-encoded images
