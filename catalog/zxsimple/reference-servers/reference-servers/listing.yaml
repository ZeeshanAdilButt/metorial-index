description: Retrieve and process web content from URLs, converting HTML to
  markdown format for easier extraction and consumption by language models. It
  supports content truncation and allows for customized starting points for
  content extraction.
skills:
  - Fetch content from web pages
  - Convert HTML to markdown
  - Truncate responses
  - Specify start index for content extraction
  - Use customizable user agents
