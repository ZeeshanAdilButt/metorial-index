description: Perform CRUD operations on Postgres databases with a focus on
  automation and safety. Features include a read-only mode and query plan checks
  using the `EXPLAIN` statement before execution.
skills:
  - CRUD operations on Postgres databases
  - Automate tasks
  - Read-only mode
  - Query plan checks with EXPLAIN statement
