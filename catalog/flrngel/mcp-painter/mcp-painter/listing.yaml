description: Create and manipulate drawings using a canvas interface, enabling
  the drawing of filled shapes and exporting of images in PNG format. Provides
  raw canvas data in JSON format for further processing.
skills:
  - Create a canvas
  - Draw filled rectangles
  - Export canvas as PNG image
  - Get raw canvas data as JSON
