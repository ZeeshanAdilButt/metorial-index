description: Manage databases with Prisma's ORM tools, automate schema
  migrations, generate type-safe query builders, and interact visually with data
  through a GUI. The Prisma MCP server enables AI agents to control Prisma
  Postgres databases by spinning up instances and running migrations.
skills:
  - Manage Postgres databases
  - Automate schema migrations
  - Generate type-safe query builders
  - Visual data interaction
  - Spin up database instances
