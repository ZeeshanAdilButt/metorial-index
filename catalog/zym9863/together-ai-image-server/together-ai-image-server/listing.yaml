description: Generates images from text prompts using Together AI's image
  generation models via the MCP protocol. It supports optional parameters for
  fine-tuning the image generation process.
skills:
  - Generate images from text prompts
  - Control generation steps
  - Specify number of images
