description: Connect to multiple managed indexes on LlamaCloud to retrieve
  information. Each tool corresponds to a specific index and allows for targeted
  querying through command-line parameters.
skills:
  - Query specific managed indexes
  - Retrieve information from defined indexes
  - Auto-generate tool names based on index names
