description: Execute JMeter tests in both GUI and non-GUI modes while capturing
  and returning execution outputs seamlessly through MCP-compatible clients.
skills:
  - Execute JMeter tests in non-GUI mode
  - Launch JMeter in GUI mode
  - Capture execution output
