description: Generates placeholder images from multiple providers, supporting
  both simple and real images as placeholders. Validates input parameters and
  returns image URLs for immediate use.
skills:
  - Generate placeholder images
  - Validate input parameters
  - Return image URLs
