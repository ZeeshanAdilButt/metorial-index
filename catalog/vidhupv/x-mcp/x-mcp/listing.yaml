description: Create drafts of X(Twitter) posts and threads using LLMs, and
  publish them directly through chat interfaces.
skills:
  - Generate X(Twitter) post drafts
  - Manage X(Twitter) threads
  - Post directly to X(Twitter) from chat
