description: Execute commands securely in Windows Subsystem for Linux (WSL) with
  built-in safety features for command validation and output formatting.
skills:
  - Execute commands in WSL
  - Validate command safety
  - Handle errors and validate output
  - Support working directories
  - Implement command timeouts
  - Detect dangerous commands
  - Sanitize commands to prevent shell injection
