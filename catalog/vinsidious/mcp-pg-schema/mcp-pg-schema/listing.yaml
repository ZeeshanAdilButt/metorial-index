description: Provides read-only access to PostgreSQL databases, enabling
  inspection of database schemas and execution of read-only SQL queries within a
  read-only transaction.
skills:
  - Execute read-only SQL queries
  - Inspect database schemas
  - Retrieve table schema information
