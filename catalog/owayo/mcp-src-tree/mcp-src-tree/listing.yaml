description: Generates a file tree of the specified directory's 'src' folder
  while adhering to rules defined in `.gitignore` to exclude certain files and
  directories. Represents the directory structure hierarchically in JSON format.
skills:
  - Generate file tree in JSON format
  - Exclude files/directories based on .gitignore
  - Hierarchical representation of directory structure
