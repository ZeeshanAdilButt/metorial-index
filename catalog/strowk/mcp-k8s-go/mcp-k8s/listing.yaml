description: Connects to Kubernetes for managing contexts, pods, events, and
  services, and facilitates fetching pod logs across specified namespaces.
skills:
  - List contexts
  - List pods
  - List events
  - List services
  - Fetch pod logs
