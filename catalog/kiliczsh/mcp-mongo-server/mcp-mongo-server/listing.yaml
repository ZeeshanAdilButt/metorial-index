description: Provides access to MongoDB databases for large language models,
  enabling inspection of collection schemas and execution of read-only queries.
skills:
  - Inspect collection schemas
  - Execute read-only MongoDB queries
  - Intelligent conversion between string IDs and MongoDB ObjectIds
