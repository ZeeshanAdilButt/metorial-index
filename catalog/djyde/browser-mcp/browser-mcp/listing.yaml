description: Interact with the browser to execute commands, modify styles, and
  access browsing history. Enables retrieval of content from the current page in
  markdown format and customization of page styles directly through an MCP
  interface.
skills:
  - Get the markdown from current browser browsing page
  - Append a CSS style to the current browser browsing page
