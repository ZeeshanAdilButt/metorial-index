description: Execute SQL queries against Google BigQuery with validation,
  ensuring that queries are safe, valid, and constrained to specific processing
  limits. Retrieve structured JSON results for analytics needs without modifying
  data.
skills:
  - Perform dry run of SQL queries
  - Validate SQL query syntax
  - Estimate processing size of queries
  - Execute SELECT queries under 1TB
  - Return query results in JSON format
