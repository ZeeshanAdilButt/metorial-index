description: Provides querying capabilities for package repositories such as
  PyPI, npm, crates.io, Docker Hub, and Terraform Registry, enabling searches
  for packages, Docker images, and modules. Facilitates package discovery and
  inspection through a unified protocol interface.
skills:
  - Search for packages in package indices
  - Retrieve package metadata
  - Find Docker images
  - Locate Terraform modules
