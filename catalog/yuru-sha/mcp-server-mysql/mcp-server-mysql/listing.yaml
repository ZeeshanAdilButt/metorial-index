description: Enables inspection of MySQL database schemas and execution of
  read-only queries. Provides safe query execution within READ ONLY transactions
  and supports Docker.
skills:
  - Inspect database schemas
  - Execute read-only queries
  - Safe query execution
  - Use with Docker
  - Install via NPM package
