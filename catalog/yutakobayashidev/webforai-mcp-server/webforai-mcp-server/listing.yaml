description: Extract plain text from any web page URL and convert it to clean,
  well-formatted Markdown output. Provides robust error handling and is
  optimized for AI consumption through seamless integration with MCP clients.
skills:
  - Extract text from web pages
  - Convert HTML to Markdown
  - Process tables, links, and images
  - Provide error handling
  - Integrate with MCP clients
