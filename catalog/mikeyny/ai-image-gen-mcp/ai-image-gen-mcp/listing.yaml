description: Generate images from text prompts using Replicate's flux-schnell
  model, with configurable image parameters and the ability to save generated
  images to a specified directory.
skills:
  - Generate images from text prompts
  - Configure image parameters (resolution, aspect ratio, quality)
  - Save generated images
  - Full MCP protocol compliance
  - Error handling and validation
