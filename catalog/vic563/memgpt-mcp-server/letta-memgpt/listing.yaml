description: Implements a memory system for large language models (LLMs) with
  capabilities for chatting, retrieving conversation history, and switching
  between multiple LLM providers.
skills:
  - Send messages to LLMs
  - Retrieve conversation history
  - Clear conversation history
  - Switch between LLM providers
