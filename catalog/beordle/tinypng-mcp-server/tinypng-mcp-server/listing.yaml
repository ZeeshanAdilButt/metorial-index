description: Compress images efficiently using the TinyPNG API. Supports both
  local and remote image compression with minimal setup required.
skills:
  - Compress local images
  - Compress remote images
