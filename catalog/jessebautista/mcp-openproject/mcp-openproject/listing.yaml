description: Enables AI agents to manage projects and tasks within a self-hosted
  OpenProject instance, facilitating CRUD operations through a serverless MCP
  endpoint. Supports secure environment handling and SSE client connections via
  proxy bridging.
skills:
  - Perform CRUD operations on projects
  - Manage tasks
  - Facilitate remote development
  - Support SSE client connections
  - Handle secure environment configurations
