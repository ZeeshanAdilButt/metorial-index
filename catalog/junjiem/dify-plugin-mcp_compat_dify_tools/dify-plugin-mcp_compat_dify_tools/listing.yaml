description: Transforms Dify tools' APIs into MCP compatible APIs, supporting
  `HTTP with SSE` and `Streamable HTTP` protocols, enabling broader integration
  and usage of existing tools within the MCP framework.
skills:
  - Convert APIs from Dify to MCP format
  - Support for HTTP with SSE protocol
  - Support for Streamable HTTP protocol
