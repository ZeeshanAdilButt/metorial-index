description: Manage RunPod resources through an MCP-compatible client, enabling
  operations such as creating, listing, updating, and deleting pods, serverless
  endpoints, templates, network volumes, and container registry authentications.
skills:
  - Create pods
  - List pods
  - Update pods
  - Delete pods
  - Create serverless endpoints
  - List serverless endpoints
  - Update serverless endpoints
  - Delete serverless endpoints
  - Create templates
  - List templates
  - Update templates
  - Delete templates
  - Create network volumes
  - List network volumes
  - Update network volumes
  - Delete network volumes
  - Create container registry authentications
  - List container registry authentications
  - Get details of pods, endpoints, templates, and volumes
