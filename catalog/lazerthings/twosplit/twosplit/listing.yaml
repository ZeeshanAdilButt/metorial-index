description: Leverages multiple Claude AI instances to optimize responses by
  sending a prompt to two separate models and aggregating their outputs with a
  third model. Provides source attribution for each response and enhances the
  overall accuracy and creativity in generated content.
skills:
  - Combine AI outputs
  - Optimize responses
  - Source attribution for outputs
  - Support multiple Claude models
  - Direct response retrieval from AI instances
