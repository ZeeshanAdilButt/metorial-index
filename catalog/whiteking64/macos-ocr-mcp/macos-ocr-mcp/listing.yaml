description: Perform Optical Character Recognition (OCR) on images with the help
  of macOS's Vision framework, extracting recognized text segments, confidence
  scores, and bounding box coordinates. Suitable for applications that require
  text extraction from image files.
skills:
  - Perform OCR on images
  - Extract recognized text segments
  - Provide confidence scores
  - Return bounding box coordinates
