description: An MCP server that executes shell commands or scripts, returning
  both standard output and error messages. It supports the execution of various
  commands and allows input via stdin for enhanced functionality.
skills:
  - Run shell commands
  - Execute scripts
  - Capture STDOUT and STDERR
  - Pass input through stdin
  - Create files from stdin
