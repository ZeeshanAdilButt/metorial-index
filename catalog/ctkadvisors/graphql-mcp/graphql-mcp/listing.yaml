description: Connects to any GraphQL API, providing access to its capabilities
  through strong type safety and dynamic tool generation. Automatically
  introspects schemas, exposes operations, and supports query whitelisting for
  enhanced security.
skills:
  - Connect to GraphQL APIs
  - Automatic schema introspection
  - Generate dynamic tools for GraphQL operations
  - Handle GraphQL mutations
  - Query and mutation whitelisting
  - Support for complex GraphQL types and input objects
