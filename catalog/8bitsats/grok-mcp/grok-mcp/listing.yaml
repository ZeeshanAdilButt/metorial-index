description: MCP server for generating images using Grok's AI image generation
  capabilities, accepting text prompts and returning images as URLs or
  base64-encoded data. Supports multiple image generation requests and error
  handling, with configuration options for API keys.
skills:
  - Generate images
  - Return image URLs
  - Return base64-encoded images
  - Handle multiple image requests
  - Set API key at runtime
