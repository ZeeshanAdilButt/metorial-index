description: Query time-series data from InfluxDB using a standardized interface
  with secure read-only access through JWT authentication. Simplifies data
  retrieval by allowing users to list databases and execute queries easily.
skills:
  - Query time-series data
  - Authenticate with JWT tokens
  - List databases
  - Execute queries
