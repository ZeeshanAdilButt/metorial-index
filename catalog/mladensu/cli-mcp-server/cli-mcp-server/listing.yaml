description: Execute controlled command-line operations with features for
  command whitelisting, path validation, and execution controls to ensure secure
  operation.
skills:
  - Execute controlled CLI commands
  - Validate command paths
  - Whitelist commands for security
  - Implement security features for CLI operations
