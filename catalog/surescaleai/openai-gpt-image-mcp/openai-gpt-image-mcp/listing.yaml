description: Generate and edit images using the latest OpenAI GPT-4o and
  gpt-image-1 models with advanced prompt control. Outputs can be saved to disk
  or received in base64 format for integration with MCP-compatible clients.
skills:
  - Generate images
  - Edit images
  - Save outputs to disk
  - Return outputs as base64
  - Support advanced prompt control
