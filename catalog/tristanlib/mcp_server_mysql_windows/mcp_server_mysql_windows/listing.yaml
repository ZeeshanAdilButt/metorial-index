description: Connect to and manipulate a local MySQL database through a RESTful
  API, supporting parameterized queries to enhance security. Includes real-time
  data push capabilities via Server-Sent Events.
skills:
  - Connect to local MySQL database
  - Execute database operations via RESTful API
  - Prevent SQL injection with parameterized queries
  - Support Server-Sent Events for real-time data updates
  - Integrate as Cursor MCP service
