description: Execute shell commands securely with controlled access and resource
  limits, providing robust error handling and output capture. Features include
  command timeouts and maximum output size limits to ensure command safety and
  compliance.
skills:
  - Execute shell commands
  - Output capture
  - Error handling
  - Command timeout management
  - Resource limit enforcement
  - Prevent command injection
