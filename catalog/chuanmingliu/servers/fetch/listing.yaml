description: Fetches web content from specified URLs and converts it to markdown
  format for easier processing by language models. Supports content extraction
  with options for maximum length and starting index for truncation.
skills:
  - Fetch web content from URLs
  - Convert HTML to markdown
  - Extract content in chunks using start_index
  - Limit response size with max_length
