description: Connects directly to an iTerm session to execute commands, interact
  with REPLs, and manage terminal output efficiently. Provides control over
  terminal actions and integrates with the model for real-time task delegation
  and responses based on the terminal content.
skills:
  - Execute terminal commands
  - Interact with REPL environments
  - Send control characters (ctrl-c, ctrl-z, etc.)
  - Inspect terminal output
  - Delegate tasks to AI models in the terminal
