description: Dynamically creates and manages MCP servers, enabling efficient
  execution of server code and lifecycle management within a flexible MCP
  ecosystem. It currently supports TypeScript for tool execution and server
  management tasks.
skills:
  - Dynamic server creation
  - Tool execution on child servers
  - Server code updates
  - Server restarts
  - Removal of unnecessary servers
