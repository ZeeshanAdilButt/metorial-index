description: Manage RunPod cloud resources through an MCP-compatible client,
  enabling operations such as creating, updating, and deleting pods, serverless
  endpoints, templates, network volumes, and container registry authentications.
skills:
  - Create pods
  - List pods
  - Update pods
  - Start pods
  - Stop pods
  - Delete pods
  - Create serverless endpoints
  - List serverless endpoints
  - Update serverless endpoints
  - Delete serverless endpoints
  - Create templates
  - List templates
  - Update templates
  - Delete templates
  - Create network volumes
  - List network volumes
  - Update network volumes
  - Delete network volumes
  - Create authentications
  - List authentications
  - Delete authentications
