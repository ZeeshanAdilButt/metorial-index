description: Manages multiple MCP servers and dynamically routes requests across
  them. Fetches tool, prompt, and resource configurations from the MetaMCP App.
skills:
  - Route requests to multiple MCP servers
  - Fetch configurations from MetaMCP App
  - Manage tool and resource requests
