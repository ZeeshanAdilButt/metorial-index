description: Retrieve and process web content from URLs, converting HTML to
  markdown for easier information extraction. Enables chunked content retrieval
  for efficient parsing by LLMs.
skills:
  - Fetch web content as markdown
  - Extract information from HTML
  - Handle content truncation with start index
  - Specify maximum character limit for responses
