description: Provides read-only access to SQLite databases through the Model
  Context Protocol (MCP), allowing for querying and data exploration with
  built-in safety features and query validation.
skills:
  - Query SQLite databases
  - Validate SQL queries
  - Explore database structure
  - Return query results safely
