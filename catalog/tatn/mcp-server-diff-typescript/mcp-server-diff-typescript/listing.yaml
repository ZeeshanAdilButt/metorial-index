description: Generates unified diffs between two text strings, allowing for text
  comparison and analysis of differences. Utilizes the `diff` package for
  accurate detection and outputs results in unified diff format.
skills:
  - Generate unified diff between two text strings
