description: Retrieves and converts web content using various formats and
  rendering methods, suitable for both data extraction and web scraping tasks.
  It allows access to text-based resources and provides raw text content from
  specified URLs without additional processing.
skills:
  - Retrieve raw text content from URLs
  - Extract data from JSON, XML, CSV, TSV
  - Support for text-based web content
