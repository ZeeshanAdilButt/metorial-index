description: Query and analyze Grafana Loki logs with full LogQL support to
  access log data directly from AI assistants. Simplifies log management by
  providing formatted results in various output formats and supporting metadata
  retrieval.
skills:
  - Query logs using LogQL
  - Retrieve label values
  - Access metadata
  - Authenticate and configure via environment variables
  - Output results in different formats
