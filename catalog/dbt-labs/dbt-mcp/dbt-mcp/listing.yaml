description: Interact with dbt to execute CLI commands, query metrics from the
  semantic layer, and explore project models efficiently, streamlining data
  workflows.
skills:
  - Execute dbt CLI commands
  - Query semantic layer metrics
  - Explore dbt project models
