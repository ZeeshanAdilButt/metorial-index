description: Enables natural language querying of live NetSuite data via a
  read-only MCP interface, utilizing the CData JDBC Driver to expose NetSuite
  data as relational SQL models.
skills:
  - Query live NetSuite data
  - Access real-time information
  - Facilitate natural language interactions with data
  - Support read-only operations over MCP protocol
