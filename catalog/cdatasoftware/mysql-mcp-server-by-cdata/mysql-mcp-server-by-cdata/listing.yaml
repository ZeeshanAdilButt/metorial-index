description: Connect to MySQL databases to perform natural language queries,
  retrieve table and column metadata, and execute SQL SELECT queries through a
  simple MCP interface. Designed for LLM clients to access live MySQL data
  seamlessly.
skills:
  - Natural language querying of MySQL data
  - Retrieve table and column metadata
  - Execute SQL SELECT queries
