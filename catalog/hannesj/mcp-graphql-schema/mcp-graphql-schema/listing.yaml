description: Explore and interact with GraphQL schemas, retrieving detailed type
  definitions and simplifying field information including types and arguments.
  Search for types and fields using pattern matching, and filter out internal
  GraphQL types for cleaner results.
skills:
  - Load GraphQL schema files
  - Explore query, mutation, and subscription fields
  - Look up detailed type definitions
  - Search for types and fields
  - Get simplified field information
  - Filter internal types
