description: Interfacing with GraphQL APIs, enabling LLMs to execute queries and
  perform schema introspection dynamically.
skills:
  - Query GraphQL APIs
  - Schema introspection
  - Execute queries dynamically
