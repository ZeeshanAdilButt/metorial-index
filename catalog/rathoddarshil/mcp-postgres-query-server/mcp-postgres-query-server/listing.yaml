description: Provides a secure, read-only interface for executing validated SQL
  SELECT queries on a PostgreSQL database and returns structured JSON responses.
  Ensures safe data access with query timeout protection and seamless
  integration with MCP clients like Claude Desktop.
skills:
  - Execute validated SQL SELECT queries
  - Return structured JSON responses
  - Ensure query timeout protection
  - Integrate with Claude Desktop
