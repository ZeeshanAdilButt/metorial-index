description: Zero burden Model Context Protocol (MCP) server designed for CRUD
  operations on MySQL databases and tables. Supports a read-only mode and the
  option to check query plans with an `EXPLAIN` statement before execution.
skills:
  - Perform CRUD operations on MySQL databases
  - Enable read-only mode
  - Check query plans using EXPLAIN
