description: Validates and filters prompts in LLM workflows to prevent security
  risks and unauthorized inputs. It checks incoming prompts against configurable
  NOVA rules, blocking those that match high-severity security patterns and
  providing structured logging for prompt security.
skills:
  - Prompt validation
  - Security risk prevention
  - Authorization enforcement
  - Structured logging
  - Multi-backend support
