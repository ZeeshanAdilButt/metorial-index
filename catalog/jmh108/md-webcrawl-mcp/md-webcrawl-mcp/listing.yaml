description: Extracts website content and saves it as markdown files while
  mapping website structures and links efficiently, enabling batch processing of
  multiple URLs.
skills:
  - Extract website content
  - Save content as markdown
  - Map website structures
  - Process multiple URLs in batches
  - Configure output directory
