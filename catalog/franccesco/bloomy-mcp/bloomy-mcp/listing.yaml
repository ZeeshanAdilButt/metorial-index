description: Connect to Bloom Growth's GraphQL API for executing queries and
  mutations, retrieving user information, and performing schema introspection
  through the Model Context Protocol.
skills:
  - Execute GraphQL queries
  - Mutate data via GraphQL
  - Retrieve user information
  - Automatic schema introspection
  - Access GraphQL API via MCP
