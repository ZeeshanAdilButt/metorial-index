tools:
  - name: what_time_is_it
    description: Returns the current time string based on the client's IP using
      World Time API.
    inputSchema:
      type: object
      title: what_time_is_itArguments
      properties: {}
