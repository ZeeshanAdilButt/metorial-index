description: Mint ERC-20 tokens across 21 blockchains, manage transactions, and
  check token balances through an interactive interface. Provide functionalities
  to deploy tokens, query metadata, and retrieve transaction details.
skills:
  - Deploy ERC-20 tokens
  - Query token metadata
  - Initiate token transfers
  - Retrieve transaction details by hash
  - Check native token balance
  - Access token metadata via URI
