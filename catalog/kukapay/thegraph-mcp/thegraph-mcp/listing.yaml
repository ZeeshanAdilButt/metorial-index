description: Powers AI agents with indexed blockchain data, enabling them to
  fetch subgraph schemas and execute GraphQL queries against specified
  subgraphs.
skills:
  - Fetch subgraph schema
  - Execute GraphQL queries
