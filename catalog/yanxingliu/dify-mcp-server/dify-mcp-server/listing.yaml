description: A simple implementation of an MCP server enabling the invocation of
  Dify workflows through the Model Context Protocol (MCP). It supports
  configuration via environment variables or a config.yaml file for flexible
  deployment options.
skills:
  - Invoke Dify workflows
  - Integrate with tools via MCP
  - Support configuration through environment variables
  - Utilize base_url and app_sks for cloud-hosted applications
