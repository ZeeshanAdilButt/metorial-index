description: Logs chat history in a formatted Markdown file, complete with
  timestamps for each message. It allows customization of save directories and
  differentiates conversations using session IDs.
skills:
  - Save chat history as Markdown
  - Timestamp messages automatically
  - Customize save directory
  - Organize conversations by session ID
