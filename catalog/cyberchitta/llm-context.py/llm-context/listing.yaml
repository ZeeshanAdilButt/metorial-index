description: Injects relevant content from code and text projects into LLM chat
  interfaces using `.gitignore` patterns for file selection. Supports both
  clipboard workflows and direct integration through the Model Context Protocol
  (MCP).
skills:
  - Content injection from code/text projects
  - File selection using .gitignore patterns
  - Clipboard workflow management
  - Direct LLM integration via MCP
