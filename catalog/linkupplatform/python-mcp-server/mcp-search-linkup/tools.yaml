tools:
  - name: search-web
    description: Perform a web search query using Linkup. This tool is helpful for
      finding information on the web.
    inputSchema:
      type: object
      required:
        - query
      properties:
        query:
          type: string
          description: The query to search the web with. This should be a question, no
            need to write in keywords.
