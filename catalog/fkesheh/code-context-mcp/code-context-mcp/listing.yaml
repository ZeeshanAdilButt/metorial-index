description: Clone local git repositories, process branches and files, generate
  embeddings for code chunks, and perform semantic searches over code. Utilize
  local repositories for efficient code context retrieval and improve
  development workflows.
skills:
  - Clone git repositories
  - Process branches and files
  - Generate embeddings for code chunks
  - Perform semantic search over code
