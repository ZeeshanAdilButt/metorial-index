description: Expose Bazel build system functionality to AI agents, enabling
  seamless building, querying, and testing of Bazel targets. Enhances workflows
  by operating effectively in misconfigured environments without a traditional
  command-line tool.
skills:
  - Build specified Bazel targets
  - Query the dependency graph for targets
  - Run tests for specified targets
  - List available targets in the workspace
  - Fetch external dependencies
  - Set workspace path
